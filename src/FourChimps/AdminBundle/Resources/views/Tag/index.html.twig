
{% extends 'FourChimpsAdminBundle:Dashboard:dashboard.html.twig' %}

{% block body %}
        

<h1>Tag list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th> {{ ' id '|uncamelcase }}</th>
            <th> {{ ' tag '|uncamelcase }}</th>
            <th> {{ ' navigable '|uncamelcase }}</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('admin_tag_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.tag }}</td>
            <td>{{ entity.navigable }}</td>
            <td>                        <a class="btn" href="{{ path('admin_tag_show', { 'id': entity.id }) }}">show</a>                        <a class="btn" href="{{ path('admin_tag_edit', { 'id': entity.id }) }}">edit</a>            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

	<a class = "btn btn-primary" href="{{ path('admin_tag_new') }}">
		<i class="icon-white icon-plus-sign"></i>
    	Create a new entry
	</a>


{% endblock %}
